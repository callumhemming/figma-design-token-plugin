import { useState } from "react";

export const Layout = () => {
  return <div></div>;
};

const Nav = () => {
  const [openTab, setOpenTab] = useState();

  return (
    <nav>
      <button></button>
    </nav>
  );
};
